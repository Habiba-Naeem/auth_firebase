class Follower{
  final String uid;
  final String name;
  final String docID;
  Follower({
    required this.uid,
    required this.name,
    //required this.songID,
    // required this.uid,
    // required this.username,
    required this.docID,
  });
}